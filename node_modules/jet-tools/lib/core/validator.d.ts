import * as ValidatorCore from 'validatorjs';
interface ErrorMessages {
    [key: string]: string | ErrorMessages;
}
export declare class Validator {
    constructor(currentLang?: string, messages?: Record<string, ErrorMessages>);
    private initial;
    setMessages(lang: string, messages: ErrorMessages): void;
    setLang(lang: string): void;
    validate<T>(data: T, rules: ValidatorCore.Rules): Record<string, string>;
}
export {};
