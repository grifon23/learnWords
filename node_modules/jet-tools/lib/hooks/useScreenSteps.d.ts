interface IProps<T> {
    initialStep: T;
    steps: T[];
}
export declare const useScreenSteps: <T>({ initialStep, steps }: IProps<T>) => {
    step: T;
    stepsCount: number;
    nextStep: () => void;
    prevStep: () => void;
};
export {};
